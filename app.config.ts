export default defineAppConfig({
  ui: {
    primary: 'blue',
    secondary: 'green',
    gray: 'cool',
    notifications: {
      // Show toasts at the top right of the screen
      position: 'bottom-0 top-[unset]',
    },
  },
});
